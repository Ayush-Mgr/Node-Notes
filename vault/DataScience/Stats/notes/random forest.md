**A** random forest is a cluster of binary decision tree with randomness in there node structure, it is basically used to reduce the error that we get while using a single decision three, using random forest let us look at the other variance as well and it uses the different output as a base and it votes to the majority ones and the measurement ones is the selected as output

---
Tags: #machine-learning


#Advanced_Statistics_and_Models
