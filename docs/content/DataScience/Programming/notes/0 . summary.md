[[Torch]]
	installing
	
```pyhon
	pip install torch  - install the torch
	print('version',format(torch.__version__))  - tells the version 

```
	
	
Data pipeline:
[[MNIST]] 
		it is a data set of hand written in digits from 0- 9
		An image is an area of matrix with the value RGB and luminescence.
	
[[torchviz]]:
	It is used in visualization of gradient from Autograd that's it
[[OpenCV]]: 
	it allows the Python to process data on visualization stuff like images,videos.
	
[[Tensor]]: 
	A **tensor in PyTorch** is a **generalized multi-dimensional array** (scalars → vectors → matrices → higher dimensions).  like Numpy.
	With the capability of utilizing GPU 
	tensors are the **data containers** you feed into neural networks
		
[[Batches]]:
	It is the process of batching multiple data in a single tensor for efficient processing of multiple samples, most of the time we do it by I'm giving the data one more value, which would be group number
	
	 JPG →  (cv2) -> NumPy →  (torch)  -> Tensor & batching→ Neural Net.
	
	
[[autograd]]
	It  shows us the correlation of each elements of the tensor with the results using [[gradient]]
	[[0. Forward propagation]] : just model predicting (no learning)
	[[1. Backpropagation algorithm|Backward propagation]]: using the prediction and comparing with actual answer and minimizing the loss by taking weights and biases
	
[[TorchVison]]:
	It isn't companion library for the torch, which has prebuilt neural networks, data sets, and others, which is focused on visualization stufs

[[MLP]]:
	
	
[[CNN]]:
	



---
Tags: #deep-learning #neural-networks


#Miscellaneous
