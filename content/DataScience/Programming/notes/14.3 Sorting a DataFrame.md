## **Sorting a DataFrame in Pandas**

You can sort a DataFrame using the `sort_values()` or `sort_index()` methods.

### 🔹 **By Column Values**

```python
df.sort_values(by='column_name', ascending=True)
```

- `by`: Column to sort on
    
- `ascending`: `True` (default) for ascending, `False` for descending
    

### ✅ **Example:**

```python
import pandas as pd

data = {'Name': ['Alice', 'Bob', 'Charlie'], 'Age': [25, 30, 22]}
df = pd.DataFrame(data)

# Sort by Age
sorted_df = df.sort_values(by='Age')
print(sorted_df)
```

### 🔹 **By Index**

```python
df.sort_index(ascending=True)
```

---


---
Tags: #programming #tools


#Data_Science_Libraries_NumPy_Pandas_Matplotlib
